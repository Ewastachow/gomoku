module Neighborhood where

import Data.String
import Control.Lens
import Data.Set
import Board


-- nazwa(posx, posy) = [(x,y) | x <- [posx-2,posx-1,posx+1], y <- [], x >= 0 , x < 19
-- let foo 0 = 0; foo x = foo x-1
-- let board = cos board - teen zapis nie przejdzie
-- loop board = do
--		x <-
--		y <-
--		...
--		loop new board

-- to na góre to whilee true

neighbors arr x y = [







-- grosquildu github

